import React from 'react';
import {Link} from 'react-router-dom';
import './NavBar.css';

const NavBar = (props) => {
    return (
        <nav className="login navbar navbar-expand-lg navbar-light bg-light">
            <h4>S & S Farms</h4>
            
            Log In
        </nav>
    );
};

export default NavBar;